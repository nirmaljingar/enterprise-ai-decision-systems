# Benchmarks

This directory will contain benchmark manifests and emitted `results.json` files for each EADS release.

Run the example benchmark with:

```bash
python examples/supply_chain.py
```

Results are versioned and intended to be compared across releases once numerical experiments begin.
