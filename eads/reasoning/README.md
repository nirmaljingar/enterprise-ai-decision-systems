# `eads.reasoning`

**Trace:** Papers 2 and 3 — evidence-backed, context-aware reasoning.

Builds plans on top of extracted evidence.

- `eads.reasoning.reasoning.ReasoningEngine` — `List[Evidence]` + goal → `Plan`.

Status: runnable skeleton; probabilistic inference and plan scoring are planned extensions.
