# Data

This directory is intentionally empty in the repository. It is a placeholder for generated synthetic datasets and any user-supplied inputs.

All built-in examples use the deterministic synthetic generators in `eads.synthetic_data` so no real enterprise data is required.
