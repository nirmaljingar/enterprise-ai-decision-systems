# Benchmarks

Archive of benchmark manifests and per-release result summaries for the EADS research companion.

Benchmark manifests are JSON files that describe scenarios, expected outcomes, and metric targets. Results are emitted to the top-level `benchmarks/` directory and archived here by release version.
